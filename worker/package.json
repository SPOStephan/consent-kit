{
  "name": "consent-kit-backend",
  "private": true,
  "type": "module",
  "description": "consent-kit Backend: Verwaltung der Websites, zentrale Einstellungen, Protokollierung – Cloudflare Worker + D1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply consent-kit --remote",
    "db:migrate:local": "wrangler d1 migrations apply consent-kit --local"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
